<h1>Listing : <%= @listing.listing_name %></h2>

<h2>Price: <%= @listing.price %></h2>

<h2>About it: <%= @listing.summary %></h2>

<h2>Home type: <%= @listing.home_type %></h2>

<h2>Room type: <%= @listing.room_type %></h2>

<h2>City: <%= @listing.city %></h2>

<h2>Bathroom: <%= @listing.bathroom %></h2>

<h2>Bedcount: <%= @listing.bedcount %></h2>

<h2>Bedroom: <%= @listing.bedroom %></h2>

<h2>Accomodates: <%= @listing.accomodates %></h2>

<h2>Created: <%= @listing.created_at %></h2>

<h2>Updated: <%= @listing.updated_at %></h2>

<p>
<label><%= @listing.listing_name %></label>
		<% @listing.images.each do |image| %>
    	<%= image_tag image.url.to_s if image.url.present? %>
    <% end %>
</p>
<div class="btn btn-warning">
<%=	link_to "Delete this post", listing_path(@listing), method: :delete %></div>

<div class="btn btn-warning">
<%=	link_to "Edit this post", edit_listing_path %></div>

<div class="btn btn-warning">
<%= link_to "Back", root_path %>
</div>

<div class="btn btn-warning">
<%= link_to "Book this place", new_listing_reservation_path(@listing) %>
</div>

